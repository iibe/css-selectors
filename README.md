# CSS Selectors

Demonstration of CSS3 selectors work.

## Getting started

1. Install required NPM-packages:

   ```bash
   npm install / npm i
   ```

   > **Or:** `yarn add`

2. Purge Tailwind styles:

   ```bash
   npm run tailwind
   ```
